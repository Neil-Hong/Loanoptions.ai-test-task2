SELECT BROKER.NAME AS BROKER_NAME, COUNT(BROKER.NAME) AS CUSTOEMR_COUNT FROM CUSTOMER JOIN BROKER ON CUSTOMER.BROKER_ID = BROKER.ID  GROUP BY BROKER_NAME ORDER BY COUNT(BROKER_NAME) DESC, BROKER_NAME ASC;